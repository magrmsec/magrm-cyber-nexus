[build]
  command = "npm install --no-audit --no-fund && npm run build && rm -rf dist && cp -R .output/public dist"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-audit --no-fund"
