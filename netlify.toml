[build]
  command = "npm install --no-audit --no-fund && npm run build && node scripts/verify-netlify-build.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-audit --no-fund"
